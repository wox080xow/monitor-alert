apiVersion: v1
kind: Service
metadata:
  name: opensearch-dashboard
  namespace: ctbc-yaml
spec:
  type: LoadBalancer
  ports:
  - port: 5601
    targetPort: 5601
  selector:
    app: opensearch-dashboard
